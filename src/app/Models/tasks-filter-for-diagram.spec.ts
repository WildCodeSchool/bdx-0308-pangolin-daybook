import { TasksFilterForDiagram } from './tasks-filter-for-diagram';

describe('TasksFilterForDiagram', () => {
  it('should create an instance', () => {
    expect(new TasksFilterForDiagram('test', 2)).toBeTruthy();
  });
});
